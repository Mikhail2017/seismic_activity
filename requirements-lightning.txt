# Dependencies for Lightning AI / cloud nodes where PyTorch is preinstalled.
# Do NOT install torch/torchvision here — use the node image's build.
#
# Usage (on the cloud machine, from repo root):
#   bash setup_lightning.sh
# or:
#   pip install -r requirements-lightning.txt

# Core project tools
h5py>=3.10
numpy>=1.24
matplotlib>=3.7
gradio>=4.0
tqdm>=4.66

# hardpicks must be editable so TOP_DIR/config/data paths resolve.
-e git+https://github.com/mila-iqia/hardpicks.git@716d68ec8786bf6b866f6d302ea9281a23c2d4f1#egg=hardpicks

# Training extras (torch/torchvision intentionally omitted)
pytorch-lightning>=1.9,<2.3
segmentation-models-pytorch>=0.3,<0.4
einops>=0.6
scipy>=1.10
opencv-python-headless>=4.8
scikit-learn>=1.2
PyYAML>=6.0
mlflow>=2.0
GitPython>=3.1
rich>=12.0
deepdiff>=6.0
psutil>=5.9
